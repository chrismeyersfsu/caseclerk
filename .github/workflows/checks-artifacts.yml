name: checks-artifacts

on:
  push:
    branches: [main]
    paths:
      - "packages/caseclerk-artifacts/**"
      - "uv.lock"
      - "pyproject.toml"
      - ".github/workflows/_checks.yml"
      - ".github/workflows/checks-artifacts.yml"
  pull_request:
    paths:
      - "packages/caseclerk-artifacts/**"
      - "uv.lock"
      - "pyproject.toml"
      - ".github/workflows/_checks.yml"
      - ".github/workflows/checks-artifacts.yml"

jobs:
  checks:
    uses: ./.github/workflows/_checks.yml
    with:
      package: caseclerk-artifacts
