name: checks-cli

on:
  push:
    branches: [main]
    paths:
      - "packages/caseclerk-cli/**"
      - "packages/caseclerk-mcp/**"
      - "packages/caseclerk-pipeline/**"
      - "packages/caseclerk-artifacts/**"
      - "packages/caseclerk-core/**"
      - "uv.lock"
      - "pyproject.toml"
      - ".github/workflows/_checks.yml"
      - ".github/workflows/checks-cli.yml"
  pull_request:
    paths:
      - "packages/caseclerk-cli/**"
      - "packages/caseclerk-mcp/**"
      - "packages/caseclerk-pipeline/**"
      - "packages/caseclerk-artifacts/**"
      - "packages/caseclerk-core/**"
      - "uv.lock"
      - "pyproject.toml"
      - ".github/workflows/_checks.yml"
      - ".github/workflows/checks-cli.yml"

jobs:
  checks:
    uses: ./.github/workflows/_checks.yml
    with:
      package: caseclerk-cli
